services:
  - type: web
    name: youtube-downloader
    env: node
    nodeVersion: 20
    buildCommand: bash build.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
